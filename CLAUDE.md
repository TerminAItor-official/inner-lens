# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a newly initialized Node.js project. No source code, tooling, or architecture has been established yet.

## Commands

```bash
npm test       # Run tests (not yet configured)
npm start      # Start the application (not yet configured)
```

Update this file as the project takes shape — add build, lint, and test commands once tooling is chosen.
